apiVersion: v1
kind: Service
metadata:
  labels:
    app: poll
  name: poll
  namespace: default
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: poll
  type: ClusterIP
