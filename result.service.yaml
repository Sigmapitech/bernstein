apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: result
  namespace: default
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: result
  type: ClusterIP
